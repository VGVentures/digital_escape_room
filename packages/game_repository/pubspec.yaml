name: game_repository
description: Domain logic for the escape room
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  ai_client:
    path: ../ai_client
  equatable: ^2.0.5
  google_generative_ai: ^0.2.0

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.1.0
